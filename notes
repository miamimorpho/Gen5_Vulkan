TODO

font rendering
read files ( .bdf )
generate texture file
pre calculate character uv's and dimensions

struct GfxCharOffsets{
	vec2 uv[2]; // top left and bottom right
	int width;
};

create indirect buffer of a buffer of GfxCharOffsets
stretch quad by width
shift x right by width